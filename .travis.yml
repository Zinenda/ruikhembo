language: node_js
node_js:
    - "0.12"

before_install:
 - sudo apt-get update
 - sudo apt-get install ruby
 - bundle install
 - bundle exec jekyll build

deploy:
    provider: surge
    project: ./_site
    skip_cleanup: true
    domain: ruikhembo.com